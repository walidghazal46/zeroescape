<?xml version="1.0" encoding="utf-8"?>
<device-admin xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-policies>
        <!-- Prevents the app from being uninstalled while it's a device admin -->
        <wipe-data />
        <force-lock />
    </uses-policies>
</device-admin>
